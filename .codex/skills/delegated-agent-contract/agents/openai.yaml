interface:
  display_name: "Delegated Agent Contract"
  short_description: "Run outcome-scoped delegated work"
  default_prompt: "Use $delegated-agent-contract to establish the outcome, invariants, non-goals, validation, and escalation boundary, then work autonomously inside that brief."
